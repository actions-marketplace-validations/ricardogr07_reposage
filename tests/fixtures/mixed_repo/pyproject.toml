[project]
name = "mixed-fixture"
version = "0.1.0"
dependencies = [
  "fastapi>=0.115",
  "pydantic>=2.10"
]

[dependency-groups]
dev = [
  "mypy>=1.14"
]

[tool.ruff]
line-length = 100

[tool.mypy]
python_version = "3.12"

